import { StyleSheet} from "react-native";

const CustomStyle = StyleSheet.create({
    tableText:{
        fontSize:25,
        textAlign: 'center',
        fontWeight:'bold'
    },
    tableDataText:{
        fontSize:20,
        textAlign: 'center',
    },
    tableScroll:{
        marginTop: 20,
        marginHorizontal: 10
    }
});

export default CustomStyle;