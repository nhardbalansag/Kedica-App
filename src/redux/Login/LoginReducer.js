const InitialStates ={
    token: [],
    data:[],
}

export default (state = InitialStates, action) =>{
    switch(action.type){
        default :
            return{
                ...state
            }
    }
    return 
}